Persistence debug helpers

This folder contains ad-hoc Cypress helper specs used for local debugging of persistence behavior.
They are intentionally placed outside of `cypress/e2e/` to avoid accidental execution as part of the test suite.

To run locally you can copy the file into `cypress/e2e/debug/` or run it using a one-off Cypress command that points at the file.
