/**
 * cypress/step_definitions/persistence.steps.js
 *
 * All persistence steps are handled by shared.steps.js:
 *   - "the app localStorage key should contain valid JSON"
 *   - "the app localStorage data should reflect a score of {int} for question {string}"
 *   - "the user reloads the page"
 *   - "the first question row should have score chip {string} active"  (question-scoring.steps.js)
 *   - "the question table should contain {string}"  (shared)
 *   - "the filter dropdown should contain {string}"  (shared)
 *
 * No additional unique steps required for this feature.
 */
