/**
 * cypress/step_definitions/audit-log.steps.js
 *
 * All audit log steps are in shared.steps.js:
 *   - "the user clicks the audit log link"
 *   - "the audit log modal should contain an entry with action {string}"
 *   - "every audit entry should have a meta badge"
 *   - "every audit entry should have a meta time element"
 *   - "the element {string} should contain text {string}"
 *
 * No additional unique steps required for this feature.
 */
