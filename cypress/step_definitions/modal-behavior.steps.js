/**
 * cypress/step_definitions/modal-behavior.steps.js
 *
 * All modal behavior steps are handled by shared.steps.js and question-edit.steps.js:
 *   - "the user opens the {string} modal"
 *   - "the user closes the modal via the Close button"
 *   - "the user presses Escape"
 *   - "the user clicks inside the modal body"
 *   - "the element {string} should be visible / not exist in the DOM"
 *   - "exactly one {string} element should exist"
 *   - "the inline principle editor should be visible / not be visible"
 *   - "the user clicks the Edit button for the principle of the first question"
 *
 * No additional unique steps required for this feature.
 */
